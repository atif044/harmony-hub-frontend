import React from 'react';
import { LoadScript } from '@react-google-maps/api';

const MapContainer = () => {
  return (
    <LoadScript
      googleMapsApiKey=""
    >
    </LoadScript>
  );
};

export default MapContainer;
